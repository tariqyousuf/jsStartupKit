# Test agains this version of Node.js
environment:
  matrix:
  # node.js
  - nodejs_version: "6"

  #install scripts(runs after repo cloning)
    #get the latest stable version of node.js or io.js
    - ps: Install-product node $env:nodejs_version
    #install modules
    - npm install
    #post-install test scriptstes

    test_script:
    - node --version
    - npm --version
    - npm test
    

    build: off